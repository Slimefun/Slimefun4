package org.bukkit;

/**
 * Compile-only stub for {@code org.bukkit.Axis} (added in Minecraft 1.13). Not shaded; on modern
 * servers the real enum is used at runtime.
 */
public enum Axis {
    X,
    Y,
    Z;
}
