package org.bukkit.entity;

/**
 * Compile-only stub for {@code org.bukkit.entity.ZombieVillager} (Minecraft 1.9+). Not shaded; on
 * modern servers the real interface is used at runtime. Slimefun only uses it for {@code instanceof}
 * checks, so it is declared as a marker extending {@link Zombie}.
 */
public interface ZombieVillager extends Zombie {
}
