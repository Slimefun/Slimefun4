package io.papermc.paper.inventory;

/**
 * Compile-only shadow stub of Paper's {@code io.papermc.paper.inventory.ItemRarity}.
 * Not shaded; the real enum is used on modern Paper at runtime. Constants mirror Paper's.
 */
public enum ItemRarity {
    COMMON,
    UNCOMMON,
    RARE,
    EPIC;
}
