package io.papermc.paper.registry.set;

/**
 * Compile-only shadow stub of Paper's {@code io.papermc.paper.registry.set.RegistryKeySet}.
 * Not shaded; the real type is used on modern Paper at runtime.
 *
 * @param <T> the registry value type
 */
public interface RegistryKeySet<T> {
}
