package org.bukkit.entity;

/**
 * Compile-only stub for {@code org.bukkit.entity.Bee} (Minecraft 1.15+). Not shaded; on modern servers
 * the real interface is used at runtime. Slimefun only uses it for {@code instanceof}, so it is a marker
 * extending {@link LivingEntity} (a conservative supertype present on the 1.8 floor).
 */
public interface Bee extends LivingEntity {
}
