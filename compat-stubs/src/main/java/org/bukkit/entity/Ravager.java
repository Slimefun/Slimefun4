package org.bukkit.entity;

/**
 * Compile-only stub for {@code org.bukkit.entity.Ravager} (Minecraft 1.14+). Not shaded; on modern
 * servers the real interface is used at runtime. Used only for {@code instanceof}, so it is a marker
 * extending {@link LivingEntity} (a conservative supertype present on the 1.8 floor).
 */
public interface Ravager extends LivingEntity {
}
