package org.bukkit.entity;

/**
 * Compile-only stub for {@code org.bukkit.entity.Trident} (Minecraft 1.13+). Not shaded; on modern
 * servers the real interface is used at runtime. Used only for {@code instanceof} on a
 * {@link Projectile}, so it is a marker extending {@link Projectile} (present on the 1.8 floor).
 */
public interface Trident extends Projectile {
}
