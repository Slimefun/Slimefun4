package org.bukkit.entity;

/**
 * Compile-only stub for {@code org.bukkit.entity.Piglin} (Minecraft 1.16+). Not shaded; on modern
 * servers the real interface is used at runtime. Slimefun only uses it for {@code instanceof} checks
 * and casts, so it is declared as a marker extending {@link Monster}.
 */
public interface Piglin extends Monster {
}
